# Normalize line endings to LF in the repo (fixes the CRLF warnings on Windows
# and keeps the code identical when it runs in Linux/Docker).
* text=auto eol=lf

# Treat these as binary — never touch their bytes.
*.png binary
*.jpg binary
*.jpeg binary
*.pdf binary
